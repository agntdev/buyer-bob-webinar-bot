BOT_TOKEN=

# Comma-separated Telegram user ids allowed to export CSV and open Owner settings.
ADMIN_IDS=

# Chat id that receives instant new-registration alerts (user or group).
# Defaults to the first ADMIN_IDS entry when unset.
ADMIN_CHAT_ID=

# Optional Redis URL for durable storage + sessions in production (Node).
# REDIS_URL=
